package code.rakheeb;

import java.util.Arrays;

//Q.sort the array using build in sort
public class ArraysSort {
    static void main(String[] args) {
        int[]arr={30,43,54,65,42};
        Arrays.sort(arr);
        System.out.println(Arrays.toString(arr));
    }
}
